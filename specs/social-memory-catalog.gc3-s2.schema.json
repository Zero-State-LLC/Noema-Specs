{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://specs.noema.dev/social-memory-catalog.gc3-s2.schema.json",
  "title": "NOEMA Social Memory Catalog (GC3-S2)",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "catalog_id",
    "slice_id",
    "authority",
    "evidence_events",
    "ignored_events",
    "public_projection",
    "watch_projection",
    "s0_s1_watch",
    "reliable_band",
    "unknown_band",
    "reputation_scalar",
    "new_verbs",
    "dangerous_line",
    "deceptive_line",
    "forbidden_in_projection"
  ],
  "properties": {
    "schema_version": { "const": "social-memory-catalog/gc3-s2" },
    "catalog_id": { "const": "social-memory-catalog/gc3-s2" },
    "slice_id": { "const": "gc3-s2" },
    "authority": { "type": "string", "minLength": 1 },
    "rfc": { "type": "string" },
    "evidence_events": { "type": "array", "minItems": 1, "items": { "type": "string" } },
    "ignored_events": { "type": "array", "items": { "type": "string" } },
    "public_projection": { "const": true },
    "watch_projection": { "const": true },
    "s0_s1_watch": { "const": false },
    "reliable_band": { "const": false },
    "unknown_band": { "const": false },
    "reputation_scalar": { "const": false },
    "new_verbs": { "type": "array", "maxItems": 0 },
    "dangerous_line": { "type": "string", "minLength": 1 },
    "deceptive_line": { "type": "string", "minLength": 1 },
    "forbidden_in_projection": { "type": "array", "minItems": 1, "items": { "type": "string" } }
  }
}
