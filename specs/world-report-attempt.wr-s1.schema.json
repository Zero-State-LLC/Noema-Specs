{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://specs.noema.dev/world-report-attempt.wr-s1.schema.json",
  "title": "NOEMA WR-S1 org-report attempt",
  "type": "object",
  "additionalProperties": false,
  "required": ["slice_id", "catalog_id", "operation", "expected"],
  "properties": {
    "slice_id": { "const": "wr-s1" },
    "catalog_id": { "const": "world-report-catalog/wr-s1" },
    "operation": { "enum": ["REPORT"] },
    "committed_cycles": { "type": "integer", "minimum": 0 },
    "org_active": { "type": "boolean" },
    "room_hidden": { "type": "boolean" },
    "expected": {
      "type": "object",
      "required": ["outcome"],
      "properties": {
        "outcome": { "enum": ["ACCEPT", "REJECT"] },
        "kept": { "type": "integer" },
        "org_lines": { "type": "integer" },
        "reason": { "type": "string" },
        "note": { "type": "string" }
      }
    }
  }
}
