{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://specs.noema.dev/communication-attempt.gc5-s11.schema.json",
  "title": "NOEMA GC5-S11 notice expiry attempt",
  "type": "object",
  "additionalProperties": false,
  "required": ["slice_id", "catalog_id", "operation", "expected"],
  "properties": {
    "slice_id": { "const": "gc5-s11" },
    "catalog_id": { "const": "communication-catalog/gc5-s11" },
    "operation": { "enum": ["NOTICE", "EXPIRE"] },
    "room_hidden": { "type": "boolean" },
    "committed_cycles": { "type": "integer", "minimum": 0 },
    "expected": {
      "type": "object",
      "required": ["outcome"],
      "properties": {
        "outcome": { "enum": ["ACCEPT", "REJECT"] },
        "kept": { "type": "integer" },
        "reason": { "type": "string" },
        "note": { "type": "string" }
      }
    }
  }
}
