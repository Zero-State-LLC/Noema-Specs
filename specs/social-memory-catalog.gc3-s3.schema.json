{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://specs.noema.dev/social-memory-catalog.gc3-s3.schema.json",
  "title": "NOEMA Social Memory Catalog (GC3-S3)",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "catalog_id",
    "slice_id",
    "authority",
    "evidence_events",
    "public_projection",
    "watch_projection",
    "reputation_scalar",
    "new_verbs",
    "role_events",
    "traded_threshold",
    "reliable_threshold",
    "traded_line",
    "reliable_line",
    "member_line",
    "removed_line",
    "danger_line",
    "forbidden_in_projection"
  ],
  "properties": {
    "schema_version": { "const": "social-memory-catalog/gc3-s3" },
    "catalog_id": { "const": "social-memory-catalog/gc3-s3" },
    "slice_id": { "const": "gc3-s3" },
    "authority": { "type": "string", "minLength": 1 },
    "rfc": { "type": "string" },
    "evidence_events": { "type": "array", "minItems": 1, "items": { "type": "string" } },
    "public_projection": { "const": false },
    "watch_projection": { "const": false },
    "reputation_scalar": { "const": false },
    "new_verbs": { "type": "array", "maxItems": 0 },
    "role_events": { "const": false },
    "traded_threshold": { "const": 1 },
    "reliable_threshold": { "const": 3 },
    "traded_line": { "type": "string" },
    "reliable_line": { "type": "string" },
    "member_line": { "type": "string" },
    "removed_line": { "type": "string" },
    "danger_line": { "type": "string" },
    "forbidden_in_projection": { "type": "array", "minItems": 1, "items": { "type": "string" } }
  }
}
