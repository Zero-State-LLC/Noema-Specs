{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://specs.noema.dev/world-report-attempt.wr-s5.schema.json",
  "title": "NOEMA WR-S5 discovery-report attempt",
  "type": "object",
  "additionalProperties": false,
  "required": ["slice_id", "catalog_id", "operation", "expected"],
  "properties": {
    "slice_id": { "const": "wr-s5" },
    "catalog_id": { "const": "world-report-catalog/wr-s5" },
    "operation": { "enum": ["REPORT"] },
    "committed_cycles": { "type": "integer", "minimum": 0 },
    "reconstruction_public": { "type": "boolean" },
    "reconstruction_hidden": { "type": "boolean" },
    "reconstruction_private": { "type": "boolean" },
    "expected": {
      "type": "object",
      "required": ["outcome"],
      "properties": {
        "outcome": { "enum": ["ACCEPT", "REJECT"] },
        "kept": { "type": "integer" },
        "discovery_lines": { "type": "integer" },
        "reason": { "type": "string" },
        "note": { "type": "string" }
      }
    }
  }
}
