{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://specs.noema.dev/world-report-attempt.wr-s6.schema.json",
  "title": "NOEMA WR-S6 diplomacy-report attempt",
  "type": "object",
  "additionalProperties": false,
  "required": ["slice_id", "catalog_id", "operation", "expected"],
  "properties": {
    "slice_id": { "const": "wr-s6" },
    "catalog_id": { "const": "world-report-catalog/wr-s6" },
    "operation": { "enum": ["REPORT"] },
    "committed_cycles": { "type": "integer", "minimum": 0 },
    "agreement_active_public": { "type": "boolean" },
    "agreement_offered": { "type": "boolean" },
    "agreement_broken": { "type": "boolean" },
    "expected": {
      "type": "object",
      "required": ["outcome"],
      "properties": {
        "outcome": { "enum": ["ACCEPT", "REJECT"] },
        "kept": { "type": "integer" },
        "diplomacy_lines": { "type": "integer" },
        "reason": { "type": "string" },
        "note": { "type": "string" }
      }
    }
  }
}
