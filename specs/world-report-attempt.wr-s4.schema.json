{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://specs.noema.dev/world-report-attempt.wr-s4.schema.json",
  "title": "NOEMA WR-S4 crime-report attempt",
  "type": "object",
  "additionalProperties": false,
  "required": ["slice_id", "catalog_id", "operation", "expected"],
  "properties": {
    "slice_id": { "const": "wr-s4" },
    "catalog_id": { "const": "world-report-catalog/wr-s4" },
    "operation": { "enum": ["REPORT"] },
    "committed_cycles": { "type": "integer", "minimum": 0 },
    "crime_public": { "type": "boolean" },
    "crime_hidden": { "type": "boolean" },
    "crime_private": { "type": "boolean" },
    "expected": {
      "type": "object",
      "required": ["outcome"],
      "properties": {
        "outcome": { "enum": ["ACCEPT", "REJECT"] },
        "kept": { "type": "integer" },
        "crime_lines": { "type": "integer" },
        "reason": { "type": "string" },
        "note": { "type": "string" }
      }
    }
  }
}
