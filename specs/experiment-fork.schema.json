{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://specs.noema.dev/experiment-fork.schema.json",
  "title": "NOEMA Experimental World Fork",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "fork_id",
    "experiment_id",
    "source_world_id",
    "source_world_version",
    "source_snapshot_id",
    "source_ledger_head",
    "fork_cycle",
    "fork_event_boundary",
    "experimental_world_id",
    "fork_point",
    "mutates_production"
  ],
  "properties": {
    "schema_version": {
      "const": "experiment-fork/0.4"
    },
    "fork_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_.:-]+$"
    },
    "experiment_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_.:-]+$"
    },
    "source_world_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_.:-]+$"
    },
    "source_world_version": {
      "type": "string"
    },
    "source_snapshot_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_.:-]+$"
    },
    "source_ledger_head": {
      "type": "string",
      "pattern": "^sha256:[a-fA-F0-9]{64}$"
    },
    "fork_cycle": {
      "type": "integer",
      "minimum": 0
    },
    "fork_event_boundary": {
      "type": "integer",
      "minimum": 0
    },
    "experimental_world_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_.:-]+$"
    },
    "fork_point": {
      "enum": [
        "BEFORE_OBSERVATION",
        "AFTER_OBSERVATION",
        "BEFORE_ACTION",
        "AFTER_ACTION",
        "CYCLE_BOUNDARY",
        "BEFORE_FRONTIER_INJECTION",
        "AFTER_FRONTIER_INJECTION"
      ]
    },
    "mutates_production": {
      "const": false
    },
    "mid_reducer_fork_forbidden": {
      "const": true
    },
    "digest": {
      "type": "string",
      "pattern": "^sha256:[a-fA-F0-9]{64}$"
    }
  }
}
