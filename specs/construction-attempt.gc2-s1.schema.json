{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://specs.noema.dev/construction-attempt.gc2-s1.schema.json",
  "title": "NOEMA GC2-S1 route_link attempt",
  "type": "object",
  "additionalProperties": false,
  "required": ["slice_id", "catalog_id", "operation", "expected"],
  "properties": {
    "slice_id": { "const": "gc2-s1" },
    "catalog_id": { "const": "construction-catalog/gc2-s1" },
    "operation": { "enum": ["CONSTRUCT", "MOVE"] },
    "class_id": { "type": "string" },
    "room_hidden": { "type": "boolean" },
    "has_route_link": { "type": "boolean" },
    "storage": { "type": "integer", "minimum": 0 },
    "expected": {
      "type": "object",
      "required": ["outcome"],
      "properties": {
        "outcome": { "enum": ["ACCEPT", "REJECT"] },
        "move_energy": { "type": "integer" },
        "reason": { "type": "string" },
        "note": { "type": "string" }
      }
    }
  }
}
