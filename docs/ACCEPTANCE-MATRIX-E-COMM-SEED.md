# Acceptance Matrix E (Communication / GC5) Evidence Note — Design Note

**Status:** Smallest matrix coverage note. Inputs only.

**Matrix requirement:** Infrastructure damage affects communication capability/latency through deterministic world state.

**Current coverage:** COMMUNICATION-ECOLOGY.md + RFC-0009 + S1–S8 + B5 CLOSED_BY_RFC. Relay bands, boards.

**Gaps noted:** Full rumor surface still PARTIAL per prior; couples to GC2/GC6 via new seeds.

**Boundaries:** Design note. Cites GAME-COMPLETENESS-PLAN.md matrix E, COMMUNICATION-ECOLOGY.md, PR #305 + main.

Smallest unit (step 10).

## Extension Points

Non-normative communication-evidence seams; accepted GC5 slices remain closed.

- Extend matrix E with a paired relay-intact/relay-damaged run using the same declared topology, cycles and messages. Record the relevant deterministic capability/latency change, not merely a screenshot of a damaged asset.
- Preserve relay-band rules, board visibility and message provenance. Permissioned STUDY reconstruction is not an extra recipient delivery or a way to reveal undiscovered rumors in PLAY.
- Compatibility/promotion: the historical PARTIAL rumor note is not authority to reopen the accepted rumor slices. Pin the applicable RFC/catalog and reconcile the matrix row against exercised positive and negative fixtures before any status change.
- Verification proposal: include unchanged infrastructure, unreachable destination, and boundary-cycle delivery cases; compare actual delivery/observation with expected bands. Accessible communication panels should distinguish pending, delivered and unavailable using text, not color alone; localized captions leave message IDs and timestamps unchanged.
