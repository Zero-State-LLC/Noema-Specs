# v0.3 Observatory — Examples

| Package | Role |
|---------|------|
| [`examples/v03-observatory/`](../../../examples/v03-observatory/) | End-to-end baseline → Frontier pressure → shift → candidates |
| Prior | `examples/v01-strategic/`, `examples/v02-frontier/` as world lineage |

Scenario outline (illustrative, not scripted agent mandate):

```text
baseline: independent resource hoarding
frontier pressure: infra scarcity + communication constraint
observed: shared ledger artifact, coordinated transfers, protocol use after crisis
observatory: anomaly + shift + external-cognition + capability candidate (unvalidated)
```


## Extension Points

Non-normative guidance for future maintenance; this section changes no current behavior or promotion status.

- Example variants can contrast hoarding, pressure, and an observed coordination shift while retaining source trajectories, baseline identity, and the prior v0.1/v0.2 world lineage. The illustrative shared-ledger story is not a mandatory Agent Player strategy.

- Keep Observatory candidates unvalidated at this handoff. A new fixture may illustrate existing schemas, but altered detector semantics or promotion rules require their owning versioned contract; an example outcome alone cannot establish reproduced capability or alter world history.

- Validate the baseline-to-pressure-to-candidate chain and all cited digests, including a no-shift or insufficient-evidence variant. Confirm candidate labels remain bounded, ordinary WATCH reveals no detector confidence, and later Lab evidence is distinguished from this illustrative Observatory package.
