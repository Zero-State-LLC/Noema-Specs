# Experiment Variables

## Classes

`WORLD`, `AGENT`, `INFORMATION`, `RESOURCE`, `TOOL`, `SOCIAL`, `TEMPORAL`, `PROTOCOL`, `OBSERVATION`.

## Registry

Machine registry: `specs/experiment-variable-registry.v04.json`.

Claim-bearing experiments MUST use registered `variable_id`s. Arbitrary free strings are non-claim-bearing only.
