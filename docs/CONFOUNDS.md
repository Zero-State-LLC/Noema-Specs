# Confound Registry

Classes: world-state drift, agent-version drift, prompt drift, memory drift, tool-version drift, seed divergence, observation mismatch, participant mismatch, resource mismatch, timing mismatch, provider nondeterminism, external service drift.

Severe confound → `NOT_COMPARABLE` (or experiment `INVALID` if it breaks required controls).
