# Experimental Controls

Controls SHOULD include deterministic replay controls, seed controls, prompt leakage controls, instruction presence/absence checks, resource budget controls, tool availability controls, social topology controls, agent-version differential tests, architecture comparisons, negative controls, positive controls, perturbations, ablations, lesions, and counterfactual replays.

A failed or absent control MUST be reported as OBSERVED and considered when assigning capability event status.
