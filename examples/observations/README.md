# Observation Fixtures

Permissioned observation examples for v0.1 projection tests.

| File | Role |
|------|------|
| `look-room-ok.json` | Successful LOOK projection |
| `inspect-redacted.json` | INSPECT with redactions / noise metadata |

Private cognition and hidden diagnostics MUST NOT appear in `content`.
