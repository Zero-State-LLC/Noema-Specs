# v0.5 Phenomenon Compiler fixture package

Canonical shared-ledger coordination capture from a READY v0.4 Lab Result.

Ordinary flow: STUDY test result → CAPTURE AS TEST → Capturing → CAPTURED TEST.

Machine pipeline: Lab Result → eligibility → phenomenon → dependency closure → ddmin → oracle → captured test → regression.
